# Airflow Plugins Directory

This directory contains custom Airflow plugins.

Currently empty - add your custom operators, hooks, or sensors here as needed.

## Example Structure:

```
plugins/
├── operators/
│   └── custom_operator.py
├── hooks/
│   └── custom_hook.py
└── sensors/
    └── custom_sensor.py
```
